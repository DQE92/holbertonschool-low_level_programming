#include "main.h"
#include <stdio.h>
/**
 * _isupper: check si un character est Maj
 * C: le character a check
 * return 1 si Maj , 0 si non
 */

int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
return (1);
else

return (0);
}
